name: Propagate Core update

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notify-macos:
    uses: TsubameApp/Tsubame/.github/workflows/notify-core-update.yml@main
    secrets:
      automation_token: ${{ secrets.TSUBAME_AUTOMATION_TOKEN }}
